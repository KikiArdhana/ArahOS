# Fonts

- `InterVariable.woff2` — Inter (OFL license), ships with the repo. Body face.
- `Satoshi-Variable.woff2` — optional display face. Download free from
  https://www.fontshare.com/fonts/satoshi and drop the variable woff2 here
  with exactly that filename. The UI falls back to Inter automatically if
  the file is absent.
